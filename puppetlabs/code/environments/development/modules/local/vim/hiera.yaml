---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "virtual and os family"
    path: "virtual/%{facts.virtual}/%{facts.os.family}.yaml"

  - name: "common"
    path: "common.yaml"

